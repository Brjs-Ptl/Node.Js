const os = require("os");
// console.log(os); // it give many things
// console.log(os.arch()); // it give arctucture of PC here x64
// console.log(os.freemem()/(1024*1024*1024)); // Free RAM
// console.log(os.totalmem()/(1024*1024*1024)); // Total RAM
// console.log(os.hostname()); // Atomic_Attempt
console.log(os.platform()); //
console.log(os.userInfo()); //