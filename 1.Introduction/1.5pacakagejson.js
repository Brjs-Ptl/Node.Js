console.log("Package.Json");

// Package.json :- It keep the details of project in coding related like project version project name repository which pacakage you install etc

const colors = require("colors"); // when we install npm i colors then we have to call that and it give color to out output we can install many modules like this.
console.log("Hello Radhe".red); // Hello Radhe in red color


// Node js is single threaded language.
// JavaScript ia also Single Threaded Language